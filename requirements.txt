# Urðarbrunnr Voice Daemon — Phase 1 + Settings Panel
# Phase 2 additions: openwakeword, pyaudio, sounddevice
# Phase 3 additions: (ElevenLabs middleware, same stack)
# Phase 4 additions: pyotp (TOTP 2FA for Web UI)

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.0.0
